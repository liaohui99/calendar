# 任务拆分：为span元素添加圆角样式

## 任务清单

### 任务1：引入Tag组件
- **输入**：ReservationCalendar.tsx文件
- **输出**：成功引入semi-ui的Tag组件
- **实现约束**：在文件顶部的import语句中添加Tag组件
- **依赖关系**：无

### 任务2：替换日历图标span为Tag组件并添加圆角
- **输入**：已引入Tag组件的ReservationCalendar.tsx文件
- **输出**：日历图标显示为带有圆角的Tag组件
- **实现约束**：使用Tag组件，设置color为primary，borderRadius为8px
- **依赖关系**：任务1

### 任务3：为新建预约单按钮添加圆角样式
- **输入**：ReservationCalendar.tsx文件
- **输出**：新建预约单按钮显示为带有圆角的样式
- **实现约束**：为Button组件添加style属性，设置borderRadius为8px
- **依赖关系**：无

### 任务4：验证构建是否成功
- **输入**：修改后的代码
- **输出**：构建成功的结果
- **实现约束**：执行npm run build命令
- **依赖关系**：任务1、任务2、任务3

### 任务5：验证页面显示效果
- **输入**：构建成功的应用
- **输出**：页面显示正常，样式符合预期
- **实现约束**：运行开发服务器并查看页面
- **依赖关系**：任务4

## 任务依赖图

```mermaid
graph TD
    A[任务1：引入Tag组件] --> B[任务2：替换日历图标span为Tag组件并添加圆角]
    C[任务3：为新建预约单按钮添加圆角样式]
    B --> D[任务4：验证构建是否成功]
    C --> D
    D --> E[任务5：验证页面显示效果]