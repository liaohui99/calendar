## 原始需求
重启前后端后仍然出现前端日志错误，需要修复以下问题：
1. React 19 的 element.ref 使用问题
2. 日期格式化中的 YYYY 问题（虽然之前已修复部分，但仍有错误）

## 任务范围

### 包括做什么
1. 完全修复前端代码中的 element.ref 使用问题，确保符合 React 19 规范
2. 全面检查并修复所有日期格式化中使用 YYYY 的问题，替换为 yyyy
3. 验证修复后前端服务运行正常，不再出现相关错误日志

### 不包括做什么
1. 不修改后端代码
2. 不更改项目的整体架构
3. 不引入新的第三方依赖

## 疑问清单
1. 问题是否出在 Semi UI 组件库的使用上？需要进一步检查组件使用方式
2. 是否还有未检查到的文件中使用了 YYYY 日期格式化？